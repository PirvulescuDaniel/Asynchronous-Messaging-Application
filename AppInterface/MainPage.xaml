<Page
    x:Class="AppInterface.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppInterface"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        
        <!--Left pane-->
        
        <ListBox ItemsSource="{x:Bind UserViewModel.Users}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="local:UserModel">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

         <!--Right pane-->
        
        <Canvas Grid.Column="1" Background="LightGray"/>

        <Button Grid.Column="1" Click="OnAddNewUser">Add new user</Button>
        

    </Grid>

</Page>
